header#header
  #title
    .beaver.avatar
      img(src="/assets/images/beaver128.png")
    h1
      a(ui-sref="landing") Bebras
    h2
      a(ui-sref="landing") 國際資訊競賽
  ul#nav
    li
    li.user
      .name
        a(href="#") {{ user.name || '小海狸' }}
      ul.switches
        li.roles
          select(ng-model="role" name="roles"
                 ng-options="role.name for role in user.roles")
        li
          a.logout(href="#" ng-click="logout()") 登出
    li.avatar
      img(src="/assets/images/user_nil.png")
