ul.menu
  li(ng-repeat="item in data.menu" ng-class="{clicked: item == clicked}")
    a(ng-click="menu_click(item)" href="")
      span(title="{{item.desc}}") {{ item.name }}
      i(class="fa fa-fw fa-{{item.icon}}")
